#include <stack>
#include <string>
#include "BTNode.h"

using namespace std;

BTNode* makeArithmeticExpressionTree(string postfix);

int main()
{

    return 0;
}

BTNode* makeArithmeticExpressionTree(string postfix)
{
    stack<BTNode*> bt_node_ptrs;
    
    return bt_node_ptrs.top();
}